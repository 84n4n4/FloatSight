<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:fillViewport="true">
    <LinearLayout android:layout_width="wrap_content"
                  android:layout_height="wrap_content"
                  android:orientation="vertical"
                  android:layout_marginLeft="20dp"
                  android:layout_marginTop="20dp">
        <CheckBox android:id="@+id/checkbox_time"
                  style="@style/metricsCheckbox"
                  android:text="@string/time_label"
                  android:checked="true"
                  android:textColor="@color/time"
                  android:drawableStart="@drawable/ic_time"
                  android:onClick="onXAxisDialogCheckboxClicked"/>
        <CheckBox android:id="@+id/checkbox_distance"
                  style="@style/metricsCheckbox"
                  android:text="@string/distance_label"
                  android:checked="true"
                  android:textColor="@color/distance"
                  android:drawableStart="@drawable/ic_dist_tot"
                  android:onClick="onXAxisDialogCheckboxClicked"/>
    </LinearLayout>
</ScrollView>
